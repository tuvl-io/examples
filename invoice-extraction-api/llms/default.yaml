kind: AgentModel
version: v1
enabled: true
metadata:
  name: default
  description: Default chat model for structured invoice extraction
spec:
  provider: gemini
  model: gemini/gemini-3.1-flash-lite
  api_key: ${GEMINI_API_KEY}
  temperature: 0
  timeout: 60
