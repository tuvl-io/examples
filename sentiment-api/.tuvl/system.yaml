# tuvl system configuration
# Generated by `tuvl init`. Do not edit deployment_mode after first boot
# — the database lock takes precedence and a mismatch will crash startup.
kind: SystemConfig
version: v1
metadata:
  name: system
spec:
  deployment_mode: single_tenant
