kind: DataSource
version: v1
enabled: true
metadata:
  name: main_postgres
  primary: true
  description: Primary PostgreSQL database (system tables, IAM, HITL instances, pgvector store)
spec:
  type: postgresql
  driver: asyncpg
  connection:
    host: ${POSTGRES_HOST:localhost}
    port: ${POSTGRES_PORT:5432}
    database: ${POSTGRES_DB:tuvl_kyc}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  pooling:
    min_size: 5
    max_size: 20
