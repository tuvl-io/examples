kind: AgentModel
version: v1
enabled: true
metadata:
  name: default
  description: Chat model for the investigate (AutonomousAgent) and assess (Agent) steps.
spec:
  provider: gemini
  model: gemini/gemini-3.1-flash-lite
  api_key: ${GEMINI_API_KEY}
  temperature: 0.2
  timeout: 60
