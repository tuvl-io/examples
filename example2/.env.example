# Generated by `tuvl init`.
# Safe to commit. Real secrets are in .env (git-ignored).

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=example
POSTGRES_USER=tuvl_cli
POSTGRES_PASSWORD=

